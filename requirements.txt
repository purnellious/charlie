python-telegram-bot>=21.0
anthropic>=0.34.0
python-dotenv>=1.0.0
APScheduler>=3.10.0
groq>=0.9.0
feedparser>=6.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.100.0
# Pinned to exact versions, not this file's usual unpinned >=, because BUG-020's
# attachment-reading design relies on specific verified behavior of these exact
# releases (pypdf's parse-timeout-relevant DoS-fix history; python-docx==1.2.0's
# XML parser construction with resolve_entities=False, verified directly against
# its source — see core/tools/email/fetch.py's read_email_attachment()). Do not
# bump either without re-verifying that behavior against the new version's source.
pypdf==6.14.2
python-docx==1.2.0
